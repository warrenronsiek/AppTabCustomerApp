import React, {Component} from 'react';
import RegisterThree from '../redux/connectedComponents/registerPartThreeConnected'

export default class RegisterScene extends Component {
  render() {
    return (
      <RegisterThree/>
    )
  }
}